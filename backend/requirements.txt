# Core Physics Libraries (ALL WORKING - NO FALLBACKS)
# NOTE: fphysics removed - using scipy.constants instead (live scientific values)
pytest
pytest-asyncio
physipy==0.2.8
sympy>=1.11
scipy  # Let pip find a compatible pre-built version
CoolProp>=6.4

# Geometry & Mesh Processing (CRITICAL - NOT OPTIONAL)
manifold3d>=2.0.0  # C++ geometry kernel
# trimesh  # Already installed as dependency, explicit for clarity
trimesh>=4.0.0  # Mesh processing
pymeshfix  # Mesh repair
rtree  # Spatial indexing for trimesh

# LLM & AI Integration
google-genai>=0.3.0  # Gemini API
# openai, groq already listed below

# PDF & Document Processing
pymupdf>=1.23.0  # fitz import - fast PDF processing

# CAD & Manufacturing
cadquery>=2.4.0  # OCCT Python wrapper

# Machine Learning & RL
gymnasium>=0.29.0  # RL environments
networkx>=3.0  # Graph processing

# Async HTTP
aiohttp>=3.9.0  # Async HTTP client
requests>=2.31.0  # Sync HTTP client (still needed)

# Agent Orchestration
langgraph>=0.2.0  # LangGraph workflow engine
langchain-core>=0.2.0  # Core LangChain

# Additional Physics Libraries
pint>=0.20  # Units management
astropy>=5.0  # Astronomical constants and units
pydy>=0.7.0  # Symbolic dynamics and kinematics
scikit-fem>=8.0  # Finite element analysis
uncertainties>=3.1  # Error propagation

# Advanced Libraries (Phase 2.4)
pymatgen>=2024.1  # Materials science and crystal structures ✓ INSTALLED
# FEniCS REMOVED - requires conda/Docker, not compatible with pip
# If needed: conda install -c conda-forge fenics
# Alternative: use scikit-fem (already included above) for FEA
# torch>=2.1 - Required by the_well, install from pytorch.org (not on PyPI for Python 3.13)
# the_well - PolymathicAI 15TB physics dataset API
#   GitHub: https://github.com/PolymathicAI/the_well
#   HuggingFace: hf://datasets/polymathic-ai/[dataset_name]
#   Install: pip install the_well (requires PyTorch >=2.1)
#   Streaming API: WellDataset(well_base_path="hf://datasets/polymathic-ai/")

# PelePhysics - C++ combustion library
#   GitHub: https://github.com/AMReX-Combustion/PelePhysics
#   Docs: https://amrex-combustion.github.io/PelePhysics/
#   Requires: AMReX, SUNDIALS, GCC/GFortran, CMake
#   Installation: git clone + make (see PelePhysics/GettingStarted.html)
# myPhysicsLab - JavaScript library (frontend only, defer to UI phase)
#   GitHub: https://www.myphysicslab.com/

# Existing dependencies (assumed to be present)
fastapi
uvicorn
pydantic
numpy==2.2.6
groq
openai
python-multipart

# Additional utilities
jinja2

# Database & API Client (Phase 8 - De-hardcoding)
supabase>=2.0.0
httpx>=0.24.0
python-dotenv>=1.0.0

# Free Pricing APIs
yfinance>=0.2.0

# Web scraping (optional fallback)
beautifulsoup4>=4.12.0
lxml>=4.9.0

# File Extraction Dependencies (Phase 6 - Landing & Requirements)
pdfplumber>=0.10.0
PyPDF2>=3.0.0
python-docx>=0.8.11
openpyxl>=3.1.0
pandas>=2.0.0
pillow>=10.0.0
pytesseract>=0.3.10

# Note: pytesseract requires system package:
#   macOS: brew install tesseract
#   Ubuntu: apt-get install tesseract-ocr
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki
