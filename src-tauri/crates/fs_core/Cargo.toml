[package]
name = "fs_core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
walkdir = "2.3"
chrono = { version = "0.4", features = ["serde"] }
thiserror = "1.0"
